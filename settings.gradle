dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Cie"
include ':app'

enableFeaturePreview("VERSION_CATALOGS")
include ':cieidsdk'
